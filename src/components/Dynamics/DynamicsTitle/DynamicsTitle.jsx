import style from '../DynamicsTitle/DynamicsTitle.module.scss';

const DynamicTitle = () => {
    return <h1 className={style.title}>Dynamics of expenses and savings</h1>
}

export default DynamicTitle;